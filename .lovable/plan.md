Remove the MISTER TUGA logo `<img>` from the `NyvaEmbedOverlay.tsx` header. Keep the "PAGAMENTO SEGURO" lock label and the close (X) button. Adjust the header flex layout so the remaining two items balance correctly (lock on left, close on right).

File: `src/components/checkout/NyvaEmbedOverlay.tsx`
No other changes.